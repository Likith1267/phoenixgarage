# PostgreSQL connection string supplied by your hosting provider.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Set PGSSL=false only for a local non-SSL PostgreSQL server. Keep the default SSL behavior on managed hosting.
# PGSSL=false
PORT=5000
